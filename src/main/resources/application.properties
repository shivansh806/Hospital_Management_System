spring.application.name=hospitalManagement

server.port=${PORT:8080}

# Mysql database connection
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/hospitaldb}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Abhi9693@}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate config
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT
jwt.secretKey=${JWT_SECRET:a3feb5b128509c313a21396b27eefca8a1491796d8e8a93a027cdb4d45d8b8a5}

# Dynamic Frontend Redirection URL
app.frontend.url=${FRONTEND_URL:http://localhost:5173}